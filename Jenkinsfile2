pipeline {
 agent any
 stages {
   stage('deploy') {
       kubernetesDeploy (
           configs: './bookinfo.yaml', 
           kubeConfig: [path: '/root/.bluemix/plugins/container-service/clusters/mycluster/kube-config-hou02-mycluster.yml']
        )       
   }
 }
}